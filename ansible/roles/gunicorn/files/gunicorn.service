[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=zold
Group=www-data
WorkingDirectory=/var/db/zold
ExecStart=/usr/local/bin/gunicorn --access-logfile - --workers 2 --bind unix:/var/run/zold.sock zold.wsgi:application

[Install]
WantedBy=multi-user.target
